Генерирование чисел Фибоначчи посредством динамического программирования
Линейная временная сложность другого алгоритма должна быть очевидно. Вычисление каждого из п значений выполняется как простое суммирование двух целых чисел, сложность которого, как по времени, так и по памяти, равна 0(п)
 Но более внимательное исследование процесса решения задачи показывает, что совсем не обязательно хранить все пррмежуточные результаты в течение вычисления всей по
 следовательности. Так как вычисляемое значение зависит от двух аргументов, то только их и нужно сохранять.
 Данная реализация понижает сложность по памяти до постоянной, при этом никак не ухудшая сложность по времени.
 
 804.cpp:

Пользователь вводит индекс числа Фибоначчи (n).
Проверяет, что 𝑛 неотрицательное.
Вызывает функцию fib_ultimate для вычисления n-го числа Фибоначчи.
Выводит результат.



lak.cpp:

Реализует функцию fib_ultimate:
Использует итеративный метод для вычисления n-го числа Фибоначчи.
Хранит два последних числа последовательности и обновляет их в цикле.




lak.h:

Объявляет прототип функции fib_ultimate, чтобы её можно было использовать в других файлах.


Makefile:

Автоматизирует процесс сборки:
Компилирует L804_298.cpp и lak.cpp в объектные файлы.
Собирает исполняемый файл L804_298.exe.
Удаляет временные файлы командой make clean.