// Садрисламов Реваль ПМ-201
// Программа демонстрирует использование функции Increment для инкрементации числа,

#include <iostream> // Для ввода-вывода
#include "increment.h" // Подключение заголовочного файла с объявлением функции increment

// Функция Increment реализует алгоритм инкрементации числа, описанный в задании.
int Increment(int y) {
    if (y == 0) {
        // Если y равно 0, возвращаем 1
        return 1;
    } else if (y % 2 == 1) {
        // Если y нечетное, выполняем рекурсивный вызов Increment(y / 2) и удваиваем результат
        return 2 * Increment(y / 2);
    } else {
        // Если y четное, вызываем функцию increment из increment.cpp
        return increment(y);
    }
}

int main() {
    int y; // Переменная для хранения введенного числа
    std::cout << "Введите число y: "; // Вывод приглашения для ввода числа
    std::cin >> y; // Считываем число с клавиатуры

    // Выводим результат работы функции Increment
    std::cout << "Результат Increment(" << y << "): " << Increment(y) << std::endl;

    return 0; // Завершаем выполнение программы
}
